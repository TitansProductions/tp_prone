# Installation

When opening the zip file, open tp_prone-main directory folder and inside there will be another directory folder which is called as tp_prone, this directory folder is the one that should be exported to your resources (The folder which contains fxmanifest.lua).
Add ensure tp_prone in the resources.cfg or server.cfg, depends where your scripts are located.
